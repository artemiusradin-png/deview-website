/**
 * Intro scene keyframes in `hk-hero-scene.tsx` are authored on a 0–8s logical timeline.
 * `INTRO_WALL_DURATION` is how long that story takes on screen (faster = smaller number).
 */
export const INTRO_MASTER_TIMELINE = 8;
export const INTRO_WALL_DURATION = 5;
